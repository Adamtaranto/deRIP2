name: derip2
channels:
  - conda-forge
  - bioconda
dependencies:
  - python 3.14
  - pip
  - pip:
      - "biopython >=1.80"
      - click
      - matplotlib
      - notebook
      - numpy
      - pandas
      - seaborn
      - tqdm
