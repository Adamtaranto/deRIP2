name: derip2-dev
channels:
  - conda-forge
  - bioconda
dependencies:
  - python 3.12
  - pip
  - pip:
      - "biopython >=1.80"
      - click
      - hatch
      - isort
      - matplotlib
      - numpy
      - numpydoc-validation
      - pandas
      - pre-commit
      - pytest
      - ruff
      - tqdm
